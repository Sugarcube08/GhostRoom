# GhostRoom Backend Environment Variables

# Server Port
PORT=4000

# Redis Configuration
# Format: redis://[user:password@]host:port[/db]
REDIS_URL=redis://localhost:6379

# Node Environment (development, production)
NODE_ENV=development

# Optional: Throttler Settings
THROTTLE_TTL=60
THROTTLE_LIMIT=100
